{
  "name": "vibemakers-newsite",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
